\documentclass[12pt,a4paper]{report}
\usepackage[utf8]{inputenc}
\usepackage[explicit]{titlesec}
\usepackage[dvipsnames]{xcolor}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{url}
\usepackage{amssymb}
\usepackage{makeidx}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage[many]{tcolorbox}
\usepackage{standalone}
\usepackage{animate}
\tcbuselibrary{listings}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{multicol}
\usepackage{makecell}
\usepackage{adjustbox}
\usepackage{subfig}
\usepackage{lipsum}
\usepackage{float} % Added for figure positioning

\usepackage{framed}
\usepackage{lastpage}
\usepackage{listings}
\usepackage{fancyhdr}
\usetikzlibrary{calc,arrows.meta,backgrounds,shapes.geometric,positioning,fit}
\setlength{\parindent}{0pt}
\newcommand{\msymbol}[1]{\ifmmode #1 \else $#1$\fi}

\newcommand{\daemathvariable}[1]{{\normalsize \color[RGB]{100,149,237}#1}}
\newcommand{\daemathvalue}[1]{{\normalsize \color[RGB]{153,153,255}#1}}
\newcommand{\daemath}[1]{{\large \color[RGB]{0,0,0}#1}}
\newcommand{\daered}[1]{{\normalsize \color[RGB]{234,0,33}#1}}
\newcommand{\daegreen}[1]{{\normalsize \color[RGB]{0,255,65}#1}}
\newcommand{\mmathvariable}[1]{{\begingroup  \color[RGB]{100,149,237}#1 \endgroup} }
\newcommand{\mmathvalue}[1]{{\begingroup  \color[RGB]{153,153,255}#1 \endgroup} }
\newcommand{\mmath}[1]{{\begingroup  \color[RGB]{0,0,0}#1 \endgroup} }
\newcommand{\mred}[1]{{\begingroup  \color[RGB]{234,0,33}#1 \endgroup} }
\newcommand{\mgreen}[1]{{\begingroup  \color[RGB]{0,255,65}#1 \endgroup} }
\definecolor{backgroundStroke}{RGB}{0,0,0}
\definecolor{backgroundFill}{RGB}{255,255,255}
\definecolor{backgroundText}{RGB}{0,0,0}
\definecolor{bgChapter}{RGB}{91,155,213}
\definecolor{bgSection}{RGB}{222,234,246}
\definecolor{fontSubsection}{RGB}{53,124,177}
\colorlet{shadecolor}{bgChapter}

\renewcommand{\contentsname}{Contents}
\newcommand{\pbreak}{\vskip 0.5cm}
%\renewcommand{\thesection}{\arabic{section}}

\newcommand{\daechapter}[1]{
  \chapter{#1}
}

\newcommand{\daechapterNoNum}[1]{
  \chapter*{#1}
  \addcontentsline{toc}{chapter}{#1}
}

\newcommand{\daesection}[1]{
  \section{#1}
}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codeblue}{rgb}{0.0,0.0,0.8}
\definecolor{backcolour}{rgb}{0.96,0.96,0.96}

% Style definition
\lstdefinestyle{cppstyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{codeblue},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codemagenta},
    basicstyle=\ttfamily\footnotesize, % Use a smaller typewriter font
    breakatwhitespace=false,         
    breaklines=true,                 % Wrap long lines automatically
    captionpos=b,                    % Caption at the bottom
    keepspaces=true,                 
    numbers=left,                    % Line numbers on the left
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2,                       % Tab width
    frame=single,                    % Draw a frame around the code
    rulecolor=\color{gray!30}        % Light gray frame color
}

\lstset{style=cppstyle}

\newtcolorbox{ybox}{
  colback=yellow,
  colframe=yellow,
  boxrule=0pt,
  arc=0pt,
  outer arc=0pt,
  boxsep=0pt,
  left=1pt,
  right=1pt,
  top=1pt,
  bottom=1pt
}

\newtcolorbox{gbox}{
  colback=lightgray,
  colframe=lightgray,
  boxrule=0pt,
  arc=0pt,
  outer arc=0pt,
  boxsep=0pt,
  left=1pt,
  right=1pt,
  top=1pt,
  bottom=1pt
}

\pagecolor{backgroundFill}
\color{backgroundText}
\everymath{\color{backgroundText}}

\titleformat{\chapter}[display]{\normalfont\color{white} \begin {shaded*}\bfseries}{\large\chaptername~\thechapter}{20pt}{\Large#1\end{shaded*}}

\titleformat{\section}
  {\normalfont\Large\bfseries} 
  {}{0em} 
  { 
    \colorbox{bgSection}{%
      \parbox{\dimexpr\textwidth-2\fboxsep\relax}{\thesection\quad#1}
    }
  }
  
\titleformat{\subsection}
  {\color{fontSubsection}\titlerule\vspace{1ex} \normalfont\large\bfseries}
  {\thesubsection}{1em}
  {
  	\quad #1
  }
  
\titleformat{\subsubsection}
  {\color{fontSubsection}\titlerule\vspace{1ex}\normalfont\normalsize\bfseries}
  {\thesubsubsection}{1em}
  {	
  	 \quad #1   	 
  }
 
% information about the gradwork
\def\title{Incremental Performance and Quality Analysis of Hybrid Ray Tracing}
\def\subtitle{Incremental Analysis in Vulkan Real-Time Rendering}
\def\author{De Meyer Luca}
\def\academicyear{2025-2026}


\fancyhf{}
\fancyhead[L]{\author}
\fancyfoot[L]{DAE - Graduation work 2025-2026}
\fancyfoot[R]{\thepage/\pageref{LastPage}}


\begin{document}
\pagestyle{fancy}

\begin{titlepage}
   \begin{center}
       \vspace*{1cm}
       \Huge{\title}
       \vspace{0.5cm}
       
       \Large{\subtitle}
       \vspace{1.5cm}
       
       \textbf{\author}
       \vfill
       \Large{Graduation work \academicyear}
      
     
		\begin{figure}[h]
    		\begin{minipage}[t]{0.40\textwidth}
        		\includegraphics[height=2cm]{logos/Howest_logo.png}
    		\end{minipage}\hfill
	    	\begin{minipage}[t]{0.40\textwidth}
   
   		    \centering
		        \includegraphics[height=2cm,trim=2cm 7cm 0.5cm 6cm,clip]{logos/DAE_logo.pdf}
       
    		\end{minipage}
			\vspace{0.8cm}
		\end{figure}
   \end{center}
\end{titlepage}

\tableofcontents

\daechapterNoNum{Abstract {\&} Keywords}
Real-time rendering has converged on hybrid pipelines that combine rasterization with selectively applied ray tracing,
yet developers still lack quantitative guidance on how to prioritize ray-traced features under fixed frame-time budgets. While effects such as shadows,
reflections, and global illumination can substantially improve visual fidelity, their performance costs and perceptual benefits vary widely across scenes, sampling strategies,
and denoising configurations, complicating engine-level decision making.

This study evaluates the incremental performance cost and visual impact of ray-traced shadows and reflections in a Vulkan-based hybrid renderer. We analyze G-buffer–guided ray generation,
SVGF denoising, and screen-space fallbacks across two representative scenes at 1080p resolution, targeting real-time frame budgets on contemporary mid-range GPUs.
Visual quality is assessed against a path-traced reference using perceptual similarity metrics (LPIPS, SSIM), while GPU profiling is used to measure feature-level costs.

By combining these measurements, we derive a "quality-per-millisecond" metric that enables direct comparison of hybrid rendering configurations.
Our results demonstrate that G-buffer–guided ray tracing at extremely low sample counts (1 SPP), when paired with spatiotemporal denoising, offers the most favorable balance between perceptual quality and performance,
rendering higher sampling rates redundant for real-time applications.
\begin{gbox}
\textbf{Keywords:} Real-time rendering, Hybrid Ray Tracing, Vulkan, Shadows, Reflections, Performance Analysis, Perceptual Quality, LPIPS.
\end{gbox}

\daechapterNoNum{Preface}
This graduation project investigates the incremental integration of ray tracing into real-time rendering pipelines, with a specific focus on balancing visual quality and performance under fixed frame-time constraints.
Rather than pursuing fully path-traced rendering, the work explores hybrid strategies that selectively combine rasterization with ray-traced shadows and reflections, reflecting the practical design choices made in modern game engines.

The project was motivated by a desire to better understand how emerging hardware-accelerated ray tracing features can be adopted pragmatically in real-time applications.
While ray tracing offers clear improvements in lighting accuracy and visual realism, its cost varies significantly depending on scene characteristics, sampling strategies, and reconstruction techniques.
This work aims to provide quantitative and qualitative insights that can inform engine-level decisions when integrating ray tracing incrementally.

From a personal perspective, the project served as an opportunity to deepen technical knowledge of Vulkan-based rendering systems, GPU profiling, and spatiotemporal reconstruction techniques.
In particular, designing and instrumenting a hybrid renderer from first principles provided valuable insight into the performance characteristics of both rasterization and ray tracing workloads, as well as the trade-offs involved in real-world engine development.

The scope and structure of this thesis were shaped by both technical constraints and time limitations.
As a result, the focus is placed on shadows and reflections as representative ray-traced effects, while more complex phenomena such as global illumination are left for future work.
Despite these limitations, the project aims to present results that are both technically sound and practically relevant to developers working with hybrid real-time rendering pipelines.

\daechapterNoNum{List of Figures}

\begingroup
\let\chapter\relax
\listoffigures
\endgroup

\daechapter{Introduction}
Hardware-accelerated ray tracing is now a mature feature of contemporary consumer GPUs, forming a standard component of real-time rendering pipelines across both high-end engines and commercial games. Rather than replacing rasterization,
ray tracing is typically deployed selectively within hybrid pipelines, where it augments traditional techniques to improve lighting fidelity, visibility accuracy, and geometric correctness under strict performance constraints.

Although recent hardware generations and advanced denoising techniques have enabled real-time path-traced rendering in controlled scenarios, fully converged path tracing remains impractical for most interactive applications without aggressive
spatio-temporal reconstruction. Consequently, modern engines rely on incremental ray tracing adoption, allocating a limited ray budget to specific effects such as shadows and reflections while retaining rasterization for primary visibility and shading.

Determining how to allocate this budget presents a nontrivial optimization problem. The performance cost of individual ray-traced features varies significantly with scene complexity, sampling strategy, and acceleration structure usage,
while the resulting perceptual benefit depends heavily on lighting conditions, material properties, and denoising quality. Existing evaluations primarily focus on fully path-traced pipelines or isolated techniques, providing limited guidance
for hybrid approaches.
DXR is discussed in the theoretical framework for context, but experimental evaluation in this work focuses on Vulkan.


\textbf{Research Goals and Contributions:}
\begin{itemize}
    \item Quantitative measurement of incremental ray tracing costs for shadows and reflections individually and in combination.
    \item Perceptual quality analysis of different sampling rates and denoising strategies using LPIPS, SSIM metrics.
    \item Discussion of DXR as a related API model in the theoretical framework, with Vulkan used for experimental evaluation.
    \item Quality-per-millisecond ranking of hybrid rendering strategies (G-buffer-guided, screen-space fallbacks).
    \item Practical configuration recommendations for achieving 60 FPS targets on mid-range RTX hardware.
\end{itemize}


\daechapter{Literature study / Theoretical framework}

\daesection{Rasterization Fundamentals}
Rasterization is the dominant rendering technique in real-time computer graphics, used in almost all 3D interactive applications.
The fundamental principle involves converting 3D geometry into a 2D grid of pixels. By using the GPU, which is highly optimized to rasterize millions of triangles, rasterization is fast and efficient, forming the backbone of real-time rendering.\\


Understanding rasterization requires a structured view of how modern GPUs transform scene data into pixels, which is formalized by the graphics rendering pipeline.


\subsection{Rasterization Baseline for Hybrid Ray Tracing}

The graphics rendering pipeline describes the process by which a three-dimensional
scene is transformed into a two-dimensional image from a virtual camera.
In modern real-time engines, rasterization serves as the primary mechanism for
determining visible surfaces and for generating screen-space representations of
geometry and materials.
In hybrid rendering pipelines, these rasterized outputs form the foundation on
which ray tracing and spatiotemporal reconstruction techniques operate.


\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.5cm,
    box/.style={rectangle, draw, fill=blue!20, minimum width=2cm, minimum height=1.2cm, align=center},
    arrow/.style={-{Stealth[length=3mm]}, thick}
]
    \node[box] (n1) {Application};
    \node[box, right=of n1] (n2) {Geometry\\Processing}; 
    \node[box, right=of n2] (n3) {Rasterization};
    \node[box, right=of n3] (n4) {Pixel\\Processing};
    \draw[arrow] (n1) -- (n2);
    \draw[arrow] (n2) -- (n3);
    \draw[arrow] (n3) -- (n4);
\end{tikzpicture} 
\caption{Graphics Rendering Pipeline}
\label{fig:Graphics-Rendering-Pipeline}
\end{figure}

Rendering commands are assembled on the CPU and submitted to the GPU, where vertex processing transforms geometry into clip
space. After clipping and viewport mapping, primitives are rasterized into
screen-space fragments.

During fragment shading, material properties are evaluated and per-pixel
attributes are written either to the framebuffer or to intermediate render
targets. Optional programmable stages such as tessellation or geometry shaders
may be used for adaptive detail or procedural effects; however, these stages are
not central to the hybrid rendering pipeline evaluated in this work and are
therefore omitted from further discussion.

For the purposes of this thesis, the primary role of rasterization is not the
production of a fully shaded image, but the efficient generation of screen-space
buffers encoding geometric and material information required for deferred
lighting, ray tracing, and spatiotemporal reconstruction.
\cite{akenine2018realtimerendering, learnopengl}
%real-time rendering
\daesection{Rendering Pipeline Variants}
%learn opengl, deferred-shading chapter
\subsection{Forward Rendering}
In a forward rendering pipeline, scene geometry is rasterized and shaded in a
single pass, with lighting computations performed directly during fragment
shading. This approach is conceptually simple and aligns closely with the
traditional graphics pipeline, making it straightforward to implement for
scenes with a limited number of light sources.

However, the cost of forward rendering scales with both fragment count and the
number of active lights, as each fragment must evaluate all relevant lighting
equations. As a result, forward rendering becomes inefficient in scenes with
many dynamic lights. Modern variants such as Forward+ mitigate this limitation
through screen-space light culling, though lighting computations remain tightly
coupled to fragment shading.
\cite{akenine2018realtimerendering, learnopengl}
\subsection{Deferred Rendering}
Deferred rendering separates geometry processing from lighting evaluation by
dividing the pipeline into two distinct passes: a geometry pass and a lighting
pass.

During the geometry pass, the scene is rasterized once and per-fragment
attributes such as surface normals, albedo, material parameters, and depth are
written to a set of render targets collectively referred to as the G-buffer.
Depth testing is performed during this stage; optionally, a depth pre-pass may
be executed prior to G-buffer generation to reduce overdraw and unnecessary
fragment shading.

In the lighting pass, illumination is computed in screen space by sampling the
G-buffer. Because lighting is evaluated only for visible fragments, the cost of
shading scales primarily with screen resolution rather than scene complexity,
making deferred rendering well suited for scenes with many dynamic lights.

Deferred rendering introduces trade-offs, including increased memory usage and
bandwidth requirements due to multiple render targets. Additionally, handling
transparency and hardware multisample anti-aliasing (MSAA) is non-trivial,
leading many real-time engines to employ hybrid solutions.
\cite{akenine2018realtimerendering, learnopengl}
\subsubsection{G-Buffer as Input for Hybrid Ray Tracing}
The G-buffer is a collection of screen-space render targets that store
per-fragment geometric and material information required for subsequent lighting
evaluation and hybrid ray tracing.

Typical contents include:
\begin{itemize}
    \item \textbf{World-space position vectors:} Used to reconstruct the 3D location of each fragment for lighting and shading calculations.
    \item \textbf{Surface normals:} Required for accurate lighting, reflections, and shading computations.
    \item \textbf{Albedo (diffuse color):} Stores the base color of the surface without lighting applied.
    \item \textbf{Additional material properties (optional):} Such as roughness, metallicity, and specular intensity for physically based rendering workflows.
\end{itemize}
\cite{akenine2018realtimerendering, learnopengl}
These buffers collectively allow the lighting pass to operate independently of scene geometry, enabling deferred shading and efficient evaluation of multiple light sources.\\

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.5cm,
    screen/.style={rectangle, draw=backgroundStroke, thick, minimum width=2.8cm, minimum height=1.8cm, align=center, fill=white},
    label text/.style={font=\footnotesize\sffamily, align=center},
    arrow/.style={-{Stealth[length=3mm]}, thick, color=fontSubsection, rounded corners=5pt}
]

    % Main Geometry Pass Node - Moved higher up
    \node[screen, fill=bgSection, minimum width=6cm] (geometrypass) {Geometry Pass\\ \footnotesize (Rasterizes Scene)};

    % --- G-BUFFER TARGETS ---
    % 1. Albedo
    \node[screen, below=2cm of geometrypass, xshift=-5.5cm] (albedo) {};
    \fill[red!70] ($(albedo.south west)+(0.2,0.2)$) rectangle ($(albedo.center)-(0.1,0.1)$);
    \fill[green!70] ($(albedo.center)+(0.1,0.1)$) rectangle ($(albedo.north east)-(0.2,0.2)$);
    % Distinct node for label so we can fit it later
    \node[label text, below=0.1cm of albedo] (lbl_albedo) {Target 0\\Albedo (RGB)};

    % 2. Normals
    \node[screen, right=0.5cm of albedo] (normals) {};
    \shade[left color=blue!80!black, right color=cyan!60!white] ($(normals.south west)+(0.1,0.1)$) rectangle ($(normals.north east)-(0.1,0.1)$);
    \node[label text, below=0.1cm of normals] (lbl_normals) {Target 1\\Normals (XYZ)};

    % 3. Depth
    \node[screen, right=0.5cm of normals] (depth) {};
    \shade[top color=white, bottom color=black!90] ($(depth.south west)+(0.1,0.1)$) rectangle ($(depth.north east)-(0.1,0.1)$);
    \node[label text, below=0.1cm of depth] (lbl_depth) {Depth\\Buffer (Z)};

    % 4. Material
    \node[screen, right=0.5cm of depth] (material) {};
    \fill[gray!30] ($(material.south west)+(0.1,0.1)$) rectangle ($(material.north)-(0.1,0)$);
    \fill[gray!80] ($(material.south)-(0.1,0)$) rectangle ($(material.north east)-(0.1,0.1)$);
    \node[label text, below=0.1cm of material] (lbl_material) {Target 2\\Rough/Metal};

    % --- GROUPING BOX ---
    \node[draw, dashed, inner sep=0.4cm, fit=(albedo)(lbl_albedo)(material)(lbl_material), label=below:\textbf{The G-Buffer Collection}] (gbuffergroup) {};

    % --- ARROWS ---
    \draw[arrow] (geometrypass.south) -- ++(0,-0.8) -| (albedo.north);
    \draw[arrow] (geometrypass.south) -- ++(0,-0.8) -| (normals.north);
    \draw[arrow] (geometrypass.south) -- ++(0,-0.8) -| (depth.north);
    \draw[arrow] (geometrypass.south) -- ++(0,-0.8) -| (material.north);

\end{tikzpicture}
\caption{\emph{Conceptual visualization of a G-Buffer layout created during the geometry pass.}\label{fig:gbuffer-viz}}
\end{figure}

World-space positions may be stored directly or reconstructed from depth using inverse camera matrices.
\cite{akenine2018realtimerendering, learnopengl}
\subsubsection{Lighting Pass}
The lighting pass operates entirely in screen space, reconstructing surface
properties from the G-buffer to compute lighting contributions for each visible
pixel. Because only the closest visible fragments are stored, lighting
calculations are performed solely for pixels that contribute to the final image,
improving performance in scenes with high depth complexity.

Multiple light sources can be evaluated without re-rendering geometry, making
the deferred lighting pass particularly well suited for dynamic lighting
scenarios and for integration with hybrid ray tracing techniques.

\daesection{GPU Parallelism and Pipeline Cost Factors}

These execution characteristics apply to both rasterization and ray tracing workloads.
However, ray tracing typically exhibits less coherent control flow and more irregular memory access patterns,
making divergence and bandwidth pressure dominant performance factors. The following sections introduce
hardware-accelerated ray tracing and the hybrid strategies used to control these costs under real-time frame
budgets.

\subsection{Parallel Execution Model}
\begin{itemize}
  \item GPUs exploit massive data parallelism by executing many threads in lockstep groups (warps/wavefronts).
  \item Divergence within a group reduces effective throughput, which is particularly relevant for ray traversal and shading.
\end{itemize}

\subsection{Fixed-Function vs Programmable Work}
\begin{itemize}
  \item Fixed-function stages (rasterization, interpolation, depth/stencil) are highly optimized and throughput-oriented.
  \item Programmable stages (fragment/compute/ray shaders) are flexible but more sensitive to divergence and memory behavior.
\end{itemize}

\subsection{Memory Hierarchy and Bandwidth}
\begin{itemize}
  \item Performance is often bounded by memory bandwidth when writing/reading large render targets (e.g., G-buffers).
  \item Cache behavior and access coherence strongly affect ray tracing workloads, especially with incoherent secondary rays.
\end{itemize}

\subsection{Asynchronous Compute and Overlap}
\begin{itemize}
  \item Modern GPUs can overlap compute with graphics when dependencies allow, enabling partial hiding of denoising or BVH updates.
  \item Overlap is limited by shared resources (memory bandwidth, caches, execution units) and synchronization requirements.
\end{itemize}

\cite{akenine2018realtimerendering}
\daesection{Ray Tracing \& Hybrid Strategies}
% using ray tracing gems 1 , chapter 1-2
\subsection{Ray Tracing Fundamentals}
Ray tracing is a rendering technique that simulates light transport by tracing
rays through a scene and evaluating their interactions with geometry and
materials. Rays are typically emitted from the camera to determine visible
surfaces, after which secondary rays may be spawned to evaluate effects such as
shadowing, reflections, and refractions.

A ray is commonly represented as a parametric half-line
\[
P(t) = O + tD,\quad t \ge 0
\]
where \(O\) denotes the ray origin and \(D\) its direction. In practice, rays
are traced over finite segments defined by application-specific minimum and
maximum distances.

Ray tracing algorithms are typically structured around three conceptual stages:
ray generation, ray-scene intersection, and shading. Ray generation defines the
origin and direction of rays, intersection tests determine the closest geometry
hit along each ray, and shading evaluates material response and lighting at the
intersection point. To limit computational cost, recursive ray spawning is
bounded by a maximum bounce depth.

Several ray tracing variants are commonly distinguished in the literature.
Whitted-style ray tracing assumes ideal specular surfaces and traces a limited
set of reflection, refraction, and shadow rays. Stochastic ray tracing
introduces probabilistic sampling to support effects such as glossy reflections
and soft shadows. Path tracing generalizes this approach by stochastically
sampling the full light transport equation, enabling physically based global
illumination at the cost of increased noise and computational expense.

To limit computational cost, ray intersection is accelerated using Bounding Volume Hierarchies (BVH).
As described by Galvan, these structures partition scene geometry into a tree of axis-aligned bounding boxes (AABBs),
allowing the traversal shader to cull vast amounts of geometry by testing against the boxes rather than individual triangles \cite{galvan2025}.
Modern GPUs contain dedicated hardware units to traverse these Top-Level (TLAS) and Bottom-Level (BLAS) structures efficiently.

In real-time applications, fully converged path tracing is generally
impractical under fixed frame-time budgets. Consequently, modern engines employ
hybrid ray tracing pipelines in which rasterization determines primary
visibility, while ray tracing is selectively applied to specific effects such
as shadows and reflections.
\cite{haines2019raytracinggems}
\subsection{Vulkan raytracing VS DXR}%raytracing gems 1 chap3 and raytracing gems2 chap 16

Both Vulkan Ray Tracing and DirectX Raytracing (DXR) expose hardware-accelerated ray tracing through a common conceptual model consisting of acceleration structures, programmable ray tracing shaders, and shader binding tables.
As described in Ray Tracing Gems, both APIs ultimately target the same fixed-function traversal hardware on modern GPUs and differ primarily in their API design philosophy rather than their underlying execution model.\\

DXR is implemented as an extension to DirectX~12 and is designed to integrate ray tracing into an existing graphics pipeline.
It provides higher-level abstractions for ray tracing pipelines, shader binding, and acceleration structure management, prioritizing ease of integration and reduced boilerplate for applications already built on DirectX~12.
Many resource management and synchronization details are handled implicitly by the API.\\

In contrast, Vulkan Ray Tracing follows Vulkan’s explicit control philosophy.
Acceleration structures, shader binding tables, memory allocation, and synchronization are all exposed directly to the developer.
Ray tracing pipelines are constructed in a manner similar to compute pipelines, allowing ray tracing workloads to be scheduled explicitly alongside rasterization and compute passes.
This design favors predictability and fine-grained control, making Vulkan Ray Tracing particularly suitable for engine-level implementations and research-oriented systems.\\

Despite these API-level differences, both Vulkan Ray Tracing and DXR provide access to the same ray tracing shader stages, including ray generation, miss, closest-hit, any-hit, and intersection shaders.
Ray Tracing Gems emphasizes that performance differences between the two APIs are generally attributable to application-level design choices rather than fundamental differences in the hardware ray tracing pipeline.
\par
%ray tracing gems 2
\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=1.8cm,
    box/.style={
        rectangle,
        draw,
        rounded corners,
        minimum width=3.2cm,
        minimum height=1cm,
        align=center,
        fill=green!40
    },
    graybox/.style={
        rectangle,
        draw,
        rounded corners,
        minimum width=4cm,
        minimum height=1.2cm,
        align=center,
        fill=gray!30
    },
    decision/.style={
        diamond,
        draw,
        aspect=2,
        minimum width=2.8cm,
        minimum height=1.2cm,
        align=center
    },
    arrow/.style={->, thick}
]

% Nodes
\node[box] (raygen) {Ray Generation};

\node[graybox, below=of raygen] (accel) {Acceleration\\Structure Traversal};

\node[box, right=3.5cm of accel] (anyhit) {Any Hit};
\node[box, below=of anyhit] (intersect) {Intersection};

\node[decision, below=of accel] (hit) {Hit?};

\node[box, below left=1.5cm and 1.8cm of hit] (miss) {Miss};
\node[box, below right=1.5cm and 1.8cm of hit] (closest) {Closest Hit};

% Arrows
\draw[arrow] (raygen) -- node[right] {TraceRay()} (accel);

\draw[arrow] (accel.east) -- ++(1.2,0) |- (intersect.west);
\draw[arrow] (intersect.north) -- (anyhit.south);
\draw[arrow] (anyhit.west) |- (accel.east);

\draw[arrow] (accel) -- (hit);

\draw[arrow] (hit.west) -- node[above] {No} (miss.north);
\draw[arrow] (hit.east) -- node[above] {Yes} (closest.north);

\end{tikzpicture}
\caption{Ray tracing pipeline in Vulkan with acceleration structure traversal}\label{fig:raytracing-pipeline}
\end{figure}

Both Vulkan Ray Tracing and DirectX Raytracing (DXR) expose hardware-accelerated ray traversal through similar conceptual models,
including acceleration structures, ray tracing shader stages, and shader binding tables. Prior work (e.g., Ray Tracing Gems) emphasizes that performance characteristics 
are primarily driven by ray count, coherence, and traversal complexity rather than API choice.

Consequently, this study focuses on Vulkan as a representative modern ray tracing API, with results expected to generalize to DXR-based pipelines under comparable workloads.
\cite{haines2019raytracinggems,marrs2021raytracinggems2, khronos_vulkan_guide_rt, nvidia_dxr_intro}

\subsection{Spatiotemporal Reconstruction for Low-Sample Ray Tracing}

Real-time ray tracing at interactive frame rates is constrained to extremely 
low sample counts—often a single path per pixel. At such sampling rates, 
Monte Carlo noise obscures image structure entirely, making reconstruction 
filters essential for practical deployment.

Schied et al.\ introduced Spatiotemporal Variance-Guided Filtering (SVGF), 
a reconstruction algorithm designed specifically for 1 SPP path-traced input. 
The technique combines temporal accumulation across frames with a hierarchical 
à-trous wavelet filter, using per-pixel luminance variance estimates to 
adaptively control filter strength. By leveraging G-buffer attributes 
(depth, normals, mesh IDs) as edge-stopping functions, SVGF preserves 
geometric detail while aggressively filtering stochastic noise.

Key to SVGF's effectiveness is its variance estimation strategy: temporal 
accumulation of the first and second moments of luminance provides a 
statistically meaningful variance estimate that would be impossible from 
a single spatial sample. This variance then modulates the spatial filter, 
preventing overblurring in low-variance regions while permitting aggressive 
filtering where noise dominates.

The authors report 10 ms reconstruction time at 1080p on contemporary 
hardware, with SSIM improvements of 5–47\% over prior filtering approaches. 
However, SVGF exhibits limitations relevant to hybrid pipelines: 
over-blurred specular reflections when indirect lighting is undersampled, 
temporal lag under fast motion, and incompatibility with stochastic 
primary ray effects.

SVGF is used in this study as a reference denoiser due to its algorithmic transparency, ease of integration, and prevalence in academic literature, enabling controlled comparison with newer production denoisers.
\cite{schied2017svgf}
\subsection{Production Denoisers: NRD}

Building on the foundations established by SVGF, NVIDIA Real-Time Denoisers (NRD) 
represents the current state of production-quality reconstruction for hybrid ray 
tracing pipelines. NRD is a spatio-temporal, API-agnostic library designed 
specifically for signals at 0.5--1 rays per pixel, and has been deployed in 
commercial titles including Watch Dogs: Legion, Dying Light 2, and Cyberpunk 2077.

NRD provides two primary denoiser variants:
\begin{itemize}
    \item \textbf{ReBLUR:} A blur-based denoiser using hit distance to guide 
          spatial filtering, supporting diffuse and specular signals with 
          built-in ambient/specular occlusion denoising.
    \item \textbf{ReLAX:} Designed for RTXDI (ReSTIR-based direct illumination), 
          optimized for high-frequency shadow boundaries and specular detail.
\end{itemize}

Unlike SVGF's unified approach, NRD processes diffuse and specular components 
separately, allowing signal-specific tuning. Both variants require de-modulated 
irradiance input (albedo factored out) and standard G-buffer attributes. 
Performance at 1440p on RTX 4080 hardware is reported at 2.25~ms for 
ReBLUR\_DIFFUSE\_SPECULAR in default mode, representing approximately 50\% 
improvement over equivalent SVGF configurations.\cite{nvidia_NRD_blog}

NRD's API-agnostic design supports DX11, DX12, and Vulkan through precompiled 
DXBC, DXIL, and SPIR-V shaders respectively, making it suitable for cross-API 
performance comparisons in this study.
\subsection{Hybrid Rendering Strategies}

To manage ray tracing cost under fixed frame-time budgets, modern renderers
apply ray tracing selectively and combine it with rasterized buffers and
screen-space methods. This thesis evaluates the following strategy families:
\begin{itemize}
    \item \textbf{Full-screen ray tracing:} Dispatches rays for every pixel (low SPP) with aggressive denoising.
    \item \textbf{G-buffer-guided tracing:} Uses rasterized geometry buffers to selectively dispatch rays only where needed (e.g., glossy surfaces).
    \item \textbf{Screen-space first, fallback:} SSR/SSAO when RT reflection and RTAO are turned off.
\end{itemize}



\subsection{Perceptual Quality Metrics}
Hybrid ray tracing quality is evaluated against a path-traced reference.
Because denoising introduces artifacts such as blur, temporal lag, and loss of
high-frequency detail, simple pixel-wise error metrics often correlate poorly
with perceived quality. We therefore use both a structural metric (SSIM) and a
learned perceptual metric (LPIPS) to better capture visually meaningful
differences in shadows and specular reflections.
\begin{itemize}
    \item \textbf{LPIPS:} Learned Perceptual Image Patch Similarity. Uses deep neural networks trained on human perceptual judgments.
    \item \textbf{SSIM:} Structural Similarity Index. Measures structural information preservation.
\end{itemize}
\subsection{Cost Characteristics of Ray-Traced Shadows and Reflections}
The performance cost of ray tracing depends strongly on ray count, ray
coherence, and scene traversal complexity. Shadow rays are typically short,
single-bounce queries with a clear termination criterion (visibility to a light),
making their cost primarily driven by light count, shadow distance, and BVH
occlusion complexity. In hybrid pipelines, shadow denoising is often less
expensive than reflection denoising due to lower signal variance and fewer
high-frequency view-dependent features.

Shadow rays are typically short, single-bounce queries. However, full-resolution ray-traced
shadows can be prohibitively expensive; Anagnostou demonstrates that naively ray tracing shadows for a full frame can cost upwards of 92~ms,
whereas a hybrid approach using edge-focused tracing and reconstruction can reduce this to ~15~ms \cite{anagnostou2021}.
This immense disparity necessitates the use of hybrid filtering techniques.

Ray-traced reflections are generally more expensive because reflection rays are
often less coherent (especially on rough surfaces), may require longer
traversals, and produce high-variance specular signals that demand stronger
spatiotemporal reconstruction. Reflection cost is therefore influenced by
material roughness distribution, view-dependent ray directions, and the
percentage of pixels that require fallback beyond screen space. These
differences motivate evaluating shadows and reflections separately and in
combination when budgeting ray tracing time.
\cite{akenine2018realtimerendering, haines2019raytracinggems, schied2017svgf}

\subsection{Limitations of Traditional Image Quality Metrics}
Traditional image quality metrics such as MSE and PSNR operate on per-pixel
differences and assume that small spatial shifts or changes in texture detail
are perceptually minor. In hybrid ray tracing pipelines, however, denoisers can
introduce structured artifacts such as temporal ghosting, specular smearing,
and loss of high-frequency detail that may be visually objectionable despite
low pixel-wise error. Conversely, stochastic noise may produce large pixel-wise
error while remaining less perceptually disruptive once temporally filtered.

Because the goal of this thesis is to quantify perceptual improvement per
millisecond of ray tracing overhead, evaluation requires metrics that better
correlate with human judgments of image similarity. This motivates the use of
SSIM for structural preservation and LPIPS for perceptual differences that
arise from denoising and view-dependent specular effects.
\cite{zhang2018lpips, schied2017svgf}

\daesection{Motivation for Hybrid Ray Tracing}
Hybrid rendering pipelines arise from a practical mismatch between the visual
benefits of ray-traced effects and the computational constraints of real-time
frame budgets. Rasterization remains highly efficient for primary visibility and
material evaluation, while ray tracing provides more accurate visibility and
light transport for effects that are difficult to approximate robustly in screen
space, such as hard-to-predict reflections and complex shadowing.

Consequently, modern engines rely on incremental ray tracing adoption, allocating a limited ray budget to specific effects such as shadows and reflections.
This hybrid approach has been successfully deployed in AAA titles such as Call of Duty: Modern Warfare,
where ray-traced shadows were integrated alongside traditional rasterization to balance fidelity and performance \cite{olejnik2020}.
Similarly, academic studies have demonstrated that deferred hybrid pipelines can achieve frame rates 45--118\% higher than naive full-scene ray tracing \cite{ki2023}.

However, the cost of ray tracing scales with ray count, traversal complexity,
and reconstruction overhead, and is further amplified by incoherent secondary
rays and bandwidth-heavy denoising. As a result, most real-time engines treat
ray tracing as a limited budget that must be allocated selectively across
features and scenes. This motivates evaluating incremental adoption strategies
that combine rasterized buffers (e.g., the G-buffer), selective ray dispatch,
and spatiotemporal denoising to achieve the largest perceptual improvement per
millisecond of overhead.

The remainder of this thesis investigates how ray-traced shadows and
reflections contribute to perceptual quality under fixed budgets, using
Vulkan Ray Tracing as the experimental platform. DXR is discussed as a
theoretically equivalent API model to contextualize the results.

% -----------------------------------------------------------
% RESEARCH (METHODOLOGY)
% -----------------------------------------------------------
\daechapter{Research}

\daesection{Research Questions \& Hypotheses}
\textbf{Research Questions:}
\begin{enumerate}
    \item What are the individual and combined performance costs of ray-traced shadows and reflections?
    \item How do these costs scale with resolution and scene complexity?
    \item Which hybrid strategies offer the best quality-per-millisecond ratio?
    \item How do ray-traced shadows and reflections differ in quality-per-millisecond across hybrid strategies under fixed frame budgets?
\end{enumerate}



\textbf{Hypotheses:}
\begin{itemize}
    \item \textbf{H1 (Performance):} Shadows ~2ms, Reflections 4-6ms. Combined cost is not strictly additive. Costs scale super-linearly with resolution and complexity.
    \item \textbf{H2 (Strategy):} G-buffer-guided tracing is predicted to provide the best quality-cost ratio.
    \item \textbf{H3 (Complexity):} Reflection costs scale logarithmically with BVH depth; Shadow costs scale linearly with light count.
\end{itemize}


\daesection{Methodology}

\subsection{Test Scenes}
\textbf{Indoor Scene:} Sponza\\
\textbf{Specular Scene:} Metal-Spheres\\

\subsection{Variables}
\begin{gbox}
\begin{tabularx}{\textwidth}{l X}
\textbf{Variable} & \textbf{Values} \\
Resolution & 1080p\\
Features & None, Shadows, Reflections, Both \\
SPP & 1, 2, 4, 8 \\
Denoiser & SVGF \\
Strategy & Full-screen, G-buffer guided, SSR fallback \\
\end{tabularx}
\end{gbox}

\subsection{Instrumentation}
\begin{gbox}
GPU timestamps collected for Rasterization, Shadow RT, Reflection RT, Denoising, and Post-processing. 120 frames recorded, middle 60 analyzed.
\end{gbox}

\subsection{Denoising Configurations}

We evaluate four denoising strategies at each sampling rate:

\begin{itemize}
    \item \textbf{None:} Raw ray-traced output without filtering.
    \item \textbf{Temporal accumulation:} Exponential moving average 
          with motion-vector reprojection ($\alpha = 0.2$).
    \item \textbf{SVGF-like:} Temporal accumulation combined with a 
          5-level à-trous wavelet filter using variance-guided 
          edge-stopping functions, following Schied et al.
    \item \textbf{NRD:} NVIDIA Real-Time Denoisers, representing 
          current production-quality reconstruction.
\end{itemize}

The SVGF-like configuration serves as a baseline for understanding 
the quality-cost tradeoff of classical spatiotemporal filtering 
against more recent ML-influenced approaches.


\daechapter{Implementation}

\daesection{Rasterization Baseline and Deferred Rendering}
This section describes the rasterization-based foundation of the renderer and its role within the hybrid ray tracing pipeline.
Rasterization is used to determine primary visibility and to generate the screen-space buffers required for deferred lighting, ray generation, and spatiotemporal reconstruction.

This rasterization baseline serves as the reference configuration against which all ray-traced features are incrementally evaluated in the performance analysis.
\subsection{Fragment Shading in the Rasterization Pass}
During rasterization, scene geometry is processed through a traditional vertex and fragment shading pipeline. The vertex stage transforms mesh vertices into clip space using the current view and projection matrices,
while the fragment stage evaluates material properties and outputs per-pixel attributes to multiple render targets.

Rather than producing a fully shaded image, fragment shading in this pass focuses on exporting material and geometric data required for later stages.
These outputs include surface albedo, surface normals, material parameters such as roughness and metallicity, and motion vectors. By deferring lighting calculations to a later stage,
the rasterization pass remains efficient and independent of the number of light sources.

Rasterization also provides high-quality motion vectors and depth information, which are essential for temporal reprojection in both temporal anti-aliasing (TAA) and SVGF-based denoising.
\subsection{Deferred Rendering Architecture}
The renderer employs a deferred rendering architecture consisting of a depth pre-pass, a geometry pass, and a screen-space lighting pass. In the depth pre-pass, scene geometry is rasterized without shading to populate the depth buffer.
This ensures that subsequent passes operate only on visible fragments and reduces overdraw.

The geometry pass writes per-pixel data to the G-buffer, decoupling geometry processing from lighting evaluation. This structure allows lighting cost to scale primarily with screen resolution rather than scene complexity,
which is particularly important when combining rasterization with ray tracing.

Deferred rendering also serves as the backbone of the hybrid pipeline: the G-buffer generated during rasterization is reused to guide ray tracing, provide surface properties for shading ray hits, and supply edge-stopping information for denoising filters.

The renderer also maintains an unjittered depth buffer: the jittered depth aligns with subpixel sampling for TAA, while the unjittered depth provides stable geometric references for reprojection and history rejection.

Because geometry processing and lighting are decoupled, the cost of additional ray-traced effects can be isolated to specific passes without affecting the rasterization workload.
\subsection{G-buffer Layout and Data Encoding}
The G-buffer stores a set of screen-space textures encoding per-fragment attributes. In this implementation, the G-buffer contains surface albedo, world-space or view-space normals,
depth, and packed material properties such as roughness and metallicity. Surface position is reconstructed from depth and camera matrices rather than stored explicitly, reducing memory bandwidth requirements.

In addition to material attributes, the renderer outputs per-pixel motion vectors derived from current and previous frame transformations. These motion vectors are used for temporal reprojection in TAA and SVGF,
enabling stable accumulation of low-sample ray-traced signals.

The chosen G-buffer layout balances precision and bandwidth usage, providing sufficient information for lighting, ray tracing, and reconstruction without excessive memory overhead.

This design enables direct comparison between rasterized and ray-traced techniques under identical primary visibility and material conditions, ensuring that measured performance differences arise primarily from ray traversal and denoising overhead.
\subsection{Lighting Pass}
Lighting is evaluated in a full-screen pass that operates entirely in screen space. For each pixel, surface properties are reconstructed from the G-buffer and combined with lighting information to compute the final shaded color.

When ray tracing features are enabled, ray-traced shadow masks and reflection buffers are sampled during the lighting pass and integrated into the lighting equation. When ray tracing is disabled for a given feature,
screen-space alternatives such as shadow maps or screen-space reflections (SSR) are used instead. This design allows direct comparison between rasterized and ray-traced techniques under identical lighting conditions.

By centralizing lighting in a single pass, the renderer cleanly integrates rasterized and ray-traced inputs while maintaining a consistent shading model across configurations.
\daesection{Hybrid Ray Tracing Integration}
Hybrid ray tracing is integrated into the renderer as a set of optional passes that augment rasterization-based lighting.
Ray tracing is applied selectively to shadows and reflections, while rasterization remains responsible for primary visibility and material evaluation.
\begin{lstlisting}[language=C++, caption={Simplified command recording loop showing the hybrid execution flow.}, label={lst:render-loop}]
// 1. Standard Rasterization Pre-passes
depthPassUnjittered->Execute(renderContext); // For stable reprojection
depthPass->Execute(renderContext);           // Main depth

// 2. Hybrid Conditional Logic
if (registry.useRTShadows()) {
    // Ray Traced Path: Trace -> Denoise (SVGF)
    rtShadowPass->Execute(renderContext);
    svgfShadow->Execute(renderContext);
} else {
    // Fallback: Standard Raster Shadow Maps
    shadowPass->Execute(renderContext);
}

// 3. Deferred G-Buffer Generation
geometryPass->Execute(renderContext);

// 4. Ray Traced Reflections & AO
if (registry.hasAnyRTFeature()) {
    // Explicit Barrier: Transition G-Buffer for Read Access in Compute/RT shaders
    Utils::transitionImageLayoutCustom(gNormal, ..., VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL, ...);
    
    if (registry.useRTReflections()) {
        rtReflectionPass->Execute(renderContext);
        svgfReflection->Execute(renderContext); // Denoise immediately
    }
}

// 5. Screen-Space Fallbacks (if RT is off)
if (!registry.useRTReflections()) {
    ssrPass->Execute(renderContext);
}

// 6. Final Composition
lightingPass->Execute(renderContext);   // Combine all buffers
taaPass->Execute(renderContext);        // Temporal Anti-Aliasing
toneMappingPass->Execute(renderContext);
\end{lstlisting}
\subsection{G-buffer–Guided Ray Generation}
Ray generation is guided by the G-buffer to ensure that rays are only spawned for visible pixels. Rays originate from surfaces identified during rasterization, using reconstructed world-space positions and surface normals.
\begin{lstlisting}[language=C++, caption={HLSL Ray Generation shader implementing G-Buffer depth culling.}, label={lst:depth-culling}]
uint2 pixelCoords = DispatchRaysIndex().xy;
float2 uv = (float2(pixelCoords) + 0.5) / float2(DispatchRaysDimensions().xy);

// Load depth from the rasterized G-Buffer
float depth = gDepth.Load(int3(pixelCoords, 0));

// Optimization: Early exit if pixel is background (skybox)
if (depth >= 0.99999 || depth <= 0.0) {
    // Write "lit" (1.0) to masks and return immediately
    shadowMask[pixelCoords] = 1.0;
    pointShadowMask[pixelCoords] = float4(1, 1, 1, 1);
    return;
}

// If depth is valid, proceed to load Normals and TraceRay...
\end{lstlisting}

This approach avoids tracing rays for occluded geometry and ensures that ray tracing cost scales with screen resolution rather than scene complexity.
Because ray origins and directions are derived from coherent screen-space data, this strategy improves traversal coherence compared to object-space ray dispatch and reduces unnecessary ray workload.
\subsection{Ray-Traced Shadows}
Ray-traced shadows are implemented by casting shadow rays from visible surface points toward light sources. For directional lights, rays are traced along the light direction, while point lights emit rays toward the light position with a finite maximum distance.

Each shadow ray performs a single traversal of the acceleration structure and terminates upon the first intersection. The result is a binary visibility signal indicating whether the surface point is shadowed.
This signal is written to a screen-space buffer and later consumed by the lighting pass.

Because shadow rays are short and single-bounce, they exhibit relatively low variance compared to other ray-traced effects. Nevertheless, temporal accumulation and SVGF filtering are applied to stabilize the signal under low sampling rates.
\newpage
\subsection{Ray-Traced Reflections}
Ray-traced reflections are generated by emitting reflection rays from visible surface points based on the view direction and surface normal. Reflection rays may traverse larger portions of the scene and often exhibit lower coherence,
particularly on rough surfaces.

The reflected radiance returned by these rays is stored in a screen-space buffer and denoised using SVGF before being integrated into the lighting pass. Due to the high variance of reflection signals at low sample counts,
reflections require stronger spatiotemporal reconstruction than shadows.

When ray-traced reflections are disabled, the renderer falls back to screen-space reflections. This allows direct comparison between screen-space and ray-traced reflections within the same deferred lighting framework.

\daechapter{Results}
\daesection{Rasterization Baseline Performance}

Table~\ref{tab:raster-baseline} summarizes GPU timings for the rasterization-only reference configuration, averaged over the middle 60 frames of a 120-frame capture window. Total GPU time remained stable (3--4~ms in the tested scene),
indicating a steady-state workload after warm-up.

\begin{table}[H]
\centering
\caption{Rasterization-only baseline GPU timings (mean over middle 60 frames).}
\label{tab:raster-baseline}
\begin{tabular}{l r}
\hline
\textbf{Pass} & \textbf{Mean (ms)} \\
\hline
Raster/Shadow Pass & 1.592 \\
ScreenSpace/SSAO & 1.425 \\
ScreenSpace/SSR & 0.042\\
Raster/G-Buffer pass & 1.01 \\
Depth pre-pass  & 0.044 \\
Depth (unjittered) & 0.136\\
Lighting/Deferred & 0.264 \\
Post/TAA & 0.108 \\
Post/Tonemap & 0.018 \\
\textbf{Total GPU} & \textbf{3.65} \\
\hline
\end{tabular}
\end{table}
Minor passes (exposure and sharpening) each contribute below 0.05~ms on average and are omitted for clarity.

The baseline cost is dominated by rasterized shadow mapping and SSAO, which together account for the majority of frame time. All measurements in this table were collected in the Sponza test scene at 1920$\times$1080.
This baseline serves as the reference point for evaluating the incremental overhead of ray-traced features in the following sections.

\subsection{Ray-Traced Shadow Performance}

This configuration replaces rasterized shadow mapping with ray-traced shadow queries while keeping all other pipeline stages identical to the rasterization baseline.
Raster shadow passes are disabled, and a single ray-traced shadow pass followed by SVGF-based temporal filtering is enabled.

Table~\ref{tab:rt-shadows} summarizes GPU timings averaged over the middle 60 frames of a 120-frame capture window.

\begin{table}[H]
\centering
\caption{GPU timings with ray-traced shadows enabled (Sponza, 1920$\times$1080).}
\label{tab:rt-shadows}
\begin{tabular}{l r}
\hline
\textbf{Pass} & \textbf{Mean (ms)} \\
\hline
RT/Shadow Pass & 0.526 \\
SVGF/Shadow Pass & 1.256 \\
SVGF/Point Shadow Pass & 1.095 \\
Raster/G-Buffer Pass & 1.01 \\
ScreenSpace/SSAO & 1.43 \\
Lighting/Deferred & 0.26 \\
Post-processing & $<$0.20 \\
\hline
\textbf{Total GPU} & \textbf{4.14} \\
\hline
\end{tabular}
\end{table}

Compared to the rasterization baseline (3.65~ms), ray-traced shadows introduce an additional overhead of approximately 0.5~ms per frame.
While the ray traversal cost is moderate, the majority of the overhead is attributed to SVGF-based spatiotemporal denoising, which is required to stabilize the low-sample shadow signal.
Despite the increased cost, ray-traced shadows provide improved contact hardening and eliminate common shadow map artifacts such as aliasing and light leaking.

\subsection{Ray-Traced Reflection Performance}
Ray-traced reflections replace screen-space reflections while preserving the same deferred lighting and material evaluation pipeline.
Reflection rays are generated using G-buffer surface data and denoised using SVGF.

Table~\ref{tab:rt-reflections} reports GPU timings for different reflection sample counts.
\begin{table}[H]
\centering
\caption{GPU timings for ray-traced reflections at varying sample counts (Sponza, 1920$\times$1080).}
\label{tab:rt-reflections}
\begin{tabular}{l r r r}
\hline
\textbf{Pass} & \textbf{1 SPP} & \textbf{4 SPP} & \textbf{8 SPP} \\
\hline
RT/Reflections Pass & 0.554 & 1.262 & 1.724 \\
SVGF/Reflection Pass & 1.31 & 1.146 & 1.11 \\
ScreenSpace/SSR (baseline) & 0.04 & -- & -- \\
\hline
\textbf{Total GPU} & 4.14 & 4.24 & 4.47 \\
\hline
\end{tabular}
\end{table}
Compared to the raster baseline, which uses screen-space reflections at a cost of approximately 0.04~ms, ray-traced reflections introduce a substantial but controllable overhead depending on sample count.
Reflection cost scales sub-linearly with sample count due to amortization through spatiotemporal denoising.
While higher sample counts reduce temporal instability and noise, the marginal quality improvement beyond 4 SPP diminishes relative to the added cost.
These results indicate that a 1 SPP configuration combined with temporal accumulation provides the most favorable balance between performance and visual quality.
\subsection{Hybrid Configuration Comparison}
To evaluate the effectiveness of different hybrid rendering configurations, the rasterization baseline is compared against ray-traced shadow and reflection variants under identical scene and resolution settings.
Table~\ref{tab:hybrid-comparison} summarizes total GPU frame time and incremental cost relative to the raster-only baseline.

\begin{table}[H]
\centering
\caption{Comparison of hybrid rendering configurations (Sponza, 1920$\times$1080).}
\label{tab:hybrid-comparison}
\begin{tabular}{l r r}
\hline
\textbf{Configuration} & \textbf{Total GPU (ms)} & \textbf{Overhead vs Baseline (ms)} \\
\hline
Rasterization Only & 3.65 & -- \\
RT Shadows (1 SPP + SVGF) & 4.14 & +0.49 \\
RT Reflections (1 SPP + SVGF) & 4.14 & +0.49 \\
RT Reflections (4 SPP + SVGF) & 4.24 & +0.59 \\
RT Reflections (8 SPP + SVGF) & 4.47 & +0.82 \\
\hline
\end{tabular}
\end{table}
All configurations were measured using identical scenes, resolution, and capture methodology; only the enabled ray tracing features differ between configurations.

The results show that hybrid ray tracing can be integrated at a modest and predictable performance cost when combined with G-buffer–guided ray generation and spatiotemporal denoising.
Both ray-traced shadows and 1~SPP ray-traced reflections increase frame time by approximately 0.5~ms relative to the rasterization baseline, representing an overhead of roughly 13--15\%.

Increasing reflection sample count beyond 1~SPP yields diminishing returns.
While 4~SPP and 8~SPP configurations reduce temporal noise, the additional cost grows faster than the perceptual improvement, particularly after SVGF reconstruction.
This behavior confirms that temporal accumulation is the dominant factor in stabilizing low-sample ray-traced signals.

Overall, the configuration combining G-buffer–guided ray tracing at 1~SPP with spatiotemporal denoising provides the most favorable balance between visual quality and performance.

These findings validate the hybrid strategy ranking proposed earlier.
Selective ray tracing guided by rasterized visibility, combined with low sample counts and temporal denoising, consistently outperforms both pure screen-space techniques and higher-sample ray-traced variants in terms of quality-per-millisecond.
\begin{figure}[H]
    \centering
    \subfloat[\centering \textbf{Our Implementation} (Vulkan Hybrid)]{
        \includegraphics[width=0.46\textwidth]{images/Sponza_CustomRender.png}
        \label{fig:sponza-custom}
    }
    \hfill
    \subfloat[\centering \textbf{Reference} (Unreal Engine 5)]{
        \includegraphics[width=0.46\textwidth]{images/UE5_Sponza.png}
        \label{fig:sponza-ue5}
    }
    \caption{Visual validation of the hybrid renderer. Our Vulkan implementation (a) achieves lighting and shadow parity comparable to the commercial reference (b) while maintaining real-time frame rates.}
    \label{fig:comparison}
\end{figure}
\subsection{Reflection Stress Test: Metal Spheres Scene}
The metal spheres scene was used as a reflection stress test due to its high proportion of smooth, metallic surfaces.
This configuration increases the perceptual visibility of reflection noise and makes reconstruction quality more critical.

Table~\ref{tab:metal-reflections} reports measured GPU timings for ray-traced reflections under the same capture methodology
(120 frames recorded, middle 60 averaged) at 1920$\times$1080.

\begin{table}[H]
\centering
\caption{Ray-traced reflection performance in the metal spheres test scene (1920$\times$1080).}
\label{tab:metal-reflections}
\begin{tabular}{l r r r}
\hline
\textbf{Pass / Metric} & \textbf{1 SPP} & \textbf{4 SPP} & \textbf{8 SPP} \\
\hline
RT/Reflections Pass & 0.290 & 0.322 & 0.510 \\
SVGF/Reflection Pass & 1.481 & 1.481 & 1.502 \\
\hline
\textbf{Total GPU} & \textbf{4.144} & \textbf{4.241} & \textbf{4.472} \\
\hline
\end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{images/Metal-Sphere_Custom.png}
    \caption{The "Metal Spheres" stress test scene. Unlike the diffuse Sponza scene, this environment features highly coherent loss and multi-bounce specular interactions, stressing the SVGF denoiser.}
    \label{fig:metal-stress}
\end{figure}

In this scene, the reflection traversal cost increases with sample count, while the SVGF reconstruction cost remains approximately constant.
As a result, overall frame time increases modestly from 1~SPP to 8~SPP, confirming that spatiotemporal denoising is the dominant fixed cost in low-sample reflection configurations.
These results support the conclusion that 1~SPP provides the best performance tradeoff in reflection-heavy scenarios, with higher sample counts offering diminishing returns relative to their additional cost.
\newpage

\daechapter{Discussion}
Although perceptual metrics are discussed in the methodology, the optimization analysis in this section focuses on practical quality-per-cost tradeoffs derived from measured GPU timings and observed visual stability.

The results indicate that \textbf{spatiotemporal denoising dominates perceived quality at low sample counts}.
For both ray-traced shadows and reflections, increasing sample count primarily reduces noise in the raw signal, while the denoiser is responsible for the majority of visual convergence.
As a result, the marginal visual benefit of increasing sample count beyond 1~SPP diminishes rapidly under real-time frame budgets.

A key observation is that \textbf{enabling a ray-traced feature at 1~SPP consistently provides greater perceived improvement than allocating the same budget to increasing sample count}.
This is especially evident for reflections, where SVGF reconstruction stabilizes the signal effectively at 1~SPP, and higher sample counts yield progressively smaller gains relative to their cost.

These findings suggest that quality optimization in hybrid pipelines should prioritize \emph{feature coverage} over \emph{sample density}.
Selective ray tracing combined with aggressive temporal reconstruction offers the most efficient use of limited ray budgets, while higher sample counts are best reserved for offline rendering or non–real-time scenarios.

\textbf{Limitations:}
\begin{itemize}
    \item Limited to two test scenes; may not capture all scene types.
    \item Results may not generalize to future GPU architectures.
\end{itemize}


% -----------------------------------------------------------
% CONCLUSION
% -----------------------------------------------------------
\daechapter{Conclusion}
This study confirmed that hybrid ray tracing can be achieved with only a 13--15\% performance overhead (0.49ms) on mid-range hardware.
Our results demonstrate that 1 SPP with SVGF denoising is the optimal strategy, as increasing reflection sample count to 4~SPP and 8~SPP further increased cost,
while providing diminishing visible stability improvements under spatiotemporal reconstruction.
Overall, the results support using G-buffer--guided ray tracing at low sample counts combined with spatiotemporal denoising as the most practical quality--performance tradeoff for real-time applications.

\daechapter{Future Work}
The research presented in this thesis focused on the incremental cost of hard-surface reflections and direct shadowing. While these provide the most immediate visual upgrade for hybrid pipelines,
several avenues exist to extend this work toward a fully comprehensive hybrid renderer.

\daesection{Global Illumination and Diffuse Interreflection}
This study was limited to direct lighting and specular reflections. However, the most significant visual gap between rasterization and path tracing lies in indirect diffuse illumination (Global Illumination).
Future research should evaluate the integration of probe-based solutions (such as DDGI) or screen-space history reservoirs (ReSTIR GI) alongside the G-buffer-guided techniques implemented here.
Analyzing whether the "1 SPP + Denoising" strategy holds for low-frequency indirect diffuse light—which typically exhibits different variance characteristics than high-frequency specular reflections—would be a valuable
continuation of this performance analysis.

\daesection{Machine Learning-Based Reconstruction}
Our implementation relied on SVGF, a traditional wavelet-based filter. While effective, it struggled with ghosting in high-motion scenarios and blurring of fine specular details.
Modern production pipelines are increasingly adopting machine learning-based reconstruction (e.g., NVIDIA DLSS Ray Reconstruction) or advanced heuristics (NVIDIA NRD).
A comparative analysis benchmarking SVGF against these "black box" production denoisers would provide deeper insight into whether the overhead of neural inference networks
pays off in terms of LPIPS quality compared to the relatively cheap wavelet filter used in this work.

\daesection{Dynamic Quality Scaling}
Currently, the renderer uses fixed sampling rates (1, 4, or 8 SPP) selected at compile or configuration time. A more robust engine implementation would employ dynamic quality scaling.
Future work could investigate an adaptive sampler that uses the G-buffer's roughness channel or the previous frame's variance estimate to locally vary the ray count per pixel. For example,
perfectly smooth surfaces might require only 1 SPP, while rougher dielectrics could trigger 2-4 SPP to stabilize the denoiser. Implementing such a "variance-guided ray dispatch" system could potentially
lower the total frame time further by allocating the ray budget only where strictly necessary.

\daesection{Cross-Vendor Hardware Analysis}
Finally, all performance data in this thesis was derived from NVIDIA RTX hardware. Given the architectural differences in how AMD (RDNA) and Intel (Arc) handle ray traversal, the "13--15\% overhead"
figure found here may not generalize linearly. Repeating these timestamp measurements on alternative GPU architectures would help establish a more universal
"cost of hybrid ray tracing" for cross-platform developers.
\daechapter{Critical Reflection}
\daesection{Technical Challenges in Vulkan Integration}
The transition from theoretical understanding of ray tracing to a practical Vulkan implementation proved to be the most significant challenge of this graduation work.
Unlike higher-level APIs, Vulkan demands explicit management of synchronization. As noted in Mastering Graphics Programming with Vulkan, the responsibility for hazard tracking is shifted entirely to the developer,
making the correct implementation of \texttt{VkImageMemoryBarrier} and pipeline stages a primary source of complexity in modern engine development \cite{castorina2023}.

In the early stages of implementing the G-buffer-guided path, I frequently encountered synchronization validation errors and "black screen" bugs where resources were being read before write operations had completed.
Debugging these race conditions required a shift in mindset—from thinking about rendering as a linear sequence of draw calls to thinking about it as a graph of dependencies.
Mastering the usage of \texttt{VkImageMemoryBarrier} to safely transition the G-buffer from the rasterization pass to the ray generation shader was a pivotal learning moment.
It highlighted that in hybrid rendering, the complexity lies not in the ray traversal itself, but in the safe and efficient sharing of data between the raster and compute pipelines.

Shifting to ray tracing was another beast. Setting up Shader Binding Tables (SBTs) and BVH construction proved more difficult than I first thought.
The project itself is not even close to finished; while the scenes tested here work and are stable,
moving towards larger scenes like Amazon Bistro resulted in frequent GPU crashes, highlighting the fragility of research-grade implementations compared to production engines.

\daesection{The Reality of Real-Time Ray Tracing}
Another critical realization was the discrepancy between raw ray tracing performance and perceived image quality.
Initially, I underestimated the importance of denoising. My early implementations of 1~SPP reflections produced technically correct but visually unusable noise. 

Integrating the SVGF pipeline demonstrated that for real-time applications,
the "ray tracing" is often the easy part; the "reconstruction" is where the visual fidelity is actually earned.
This project forced me to dive deeper into signal processing concepts—variance estimation, temporal accumulation,
and wavelet filtering—which were not part of my initial scope but became essential to proving the hypothesis.
Realizing that a $4$~ms reflection pass is actually $1$~ms of tracing and $3$~ms of filtering fundamentally changed how I approach performance budgeting.

\daesection{Methodological Growth}
Finally, this project refined my approach to performance analysis. At the start, I relied on simple frame-rate counters, which proved insufficient for isolating specific costs.
Implementing a robust GPU timestamping system allowed me to dissect the frame and see the true cost of individual passes. This granularity was essential for the results chapter;
without it, I would not have been able to identify that increasing sample counts yields diminishing returns compared to the overhead of the denoiser.

\daesection{Conclusion}
While the final implementation successfully validates the efficiency of G-buffer-guided tracing, the journey highlighted the immense engineering overhead required to ship hybrid features.
The project successfully bridged the gap between academic ray tracing theory and the low-level engineering reality of Vulkan, providing a solid foundation for future work in engine programming.

\bibliographystyle{apalike} 
\bibliography{references}

\end{document}